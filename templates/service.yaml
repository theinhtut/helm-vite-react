apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-vite-react.fullname" . }}
  labels:
    {{- include "helm-vite-react.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-vite-react.selectorLabels" . | nindent 4 }}
